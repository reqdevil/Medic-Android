package com.example.medic.Services

import android.util.Log

class NotificationService {
    private val TAG = "Notification Service"

    companion object {
        val instance = NotificationService()
    }

    private fun decider(name: String) {
        /*
        if name exist
            removeAlarm()
        else
            createAlarm()
         */
    }

    private fun createAlarm() {

    }

    private fun removeAlarm() {

    }
}